# 分享

## Share API

React Native 内置了一个 `Share` API，可以用来分享文本信息。

```tsx
import { Share } from 'react-native'

Share.share({
  message: 'React Native | A framework for building native apps using React'
})
```
